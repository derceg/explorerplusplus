#include "stdafx.h"

/* Handles navigation. */
class NavigationManager
{
public:

	/* Loads the specified folder within the
	current tab. */
	void	LoadFolder(void);
	void	Back(void);
	void	Forward(void);
};

void NavigationManager::LoadFolder(void)
{

}